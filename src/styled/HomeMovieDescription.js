import styled from "styled-components";
import { MovieDescription } from "./StyledMovieCard";

export const HomeMovieDescription = styled(MovieDescription)`
  opacity: 1;
  background-color: rgba(0, 0, 0, 0.5);
`;
